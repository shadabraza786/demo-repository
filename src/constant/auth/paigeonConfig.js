import {rootlink} from "../configs/linkConfig";

export default {
    loginUser : `${rootlink}login/`,
    signupUser : `${rootlink}register/`,
    logoutUser : `${rootlink}logout/`,
    getUserDetails : `${rootlink}get_details/`,
    getQuestionDetails : `${rootlink}get_questions/`,
    postQuestionDetails : `${rootlink}post_user_question/`
}